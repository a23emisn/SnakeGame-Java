package Snake;

// Main entry point to launch the Snake game.
public class Main {
	public static void main(String[] args) {
		Menu gameMenu = new Menu(); // Create a new Menu object.
		gameMenu.execute(); // Call the execute method in Menu to
							// launch the game.
	}
}
